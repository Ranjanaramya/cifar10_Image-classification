# cifar10_Image-classification
CIFAR-10 image classification using a custom Convolutional Neural Network (CNN) built from scratch in TensorFlow/Keras. Includes preprocessing, data augmentation, model training, accuracy/loss visualization, confusion matrix evaluation, and custom image prediction. Fully runnable on Google Colab with GPU support.
